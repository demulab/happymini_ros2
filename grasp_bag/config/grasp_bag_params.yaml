bag_localization_node:
  ros__parameters:
    LRF_TYPE: "UTM-30LX"  # LDS-01 or UTM-30LX
    up_down: "up"  # LRFが上下反転しているか（down: 逆さま）
    estimation:
      scan_range: 70  # LiDARデータの範囲
      dispersion: 0.006  # 分散値（バッグの端）
